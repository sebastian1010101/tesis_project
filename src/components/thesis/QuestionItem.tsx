import type { ResearchQuestion } from "../../services/mappers/questionMapper";

type QuestionItemProps = {
  question: ResearchQuestion;
};

export default function QuestionItem(_props: QuestionItemProps) {
  return <div />;
}
